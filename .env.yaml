# Environment variables for the card-cookie-crawler-python function.
# IMPORTANT: For production, use Secret Manager for sensitive values like EMAIL_PASS.
STORES: 'Target=https://www.cardcookie.com/buy-target-gift-cards,Home Depot=https://www.cardcookie.com/buy-home-depot-gift-cards'
SMTP_SERVER: 'smtp.gmail.com'
SMTP_PORT: '587'
EMAIL_USER: 'your-email@gmail.com'
EMAIL_PASS: 'your-gmail-app-password' # <-- IMPORTANT: If using Gmail, use an App Password.
EMAIL_TO: 'recipient1@example.com,recipient2@example.com'
